<?xml version="1.0" encoding="utf-8"?>
<!-- Contenedor principal de tipo ConstraintLayout: permite posicionar vistas mediante restricciones relativas -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Imagen del logo (Incremento 1): 96x96dp, margen superior de 32dp y centrado horizontal -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:layout_marginTop="32dp"
        android:src="@drawable/logo_inacap"
        android:contentDescription="Logo INACAP"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Título principal (Incremento 2): "Iniciar sesión", 24sp negrita, debajo del logo -->
    <TextView
        android:id="@+id/txtTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Iniciar sesión"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/imgLogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo de entrada para Usuario (Incremento 3): ancho 0dp (match constraint) con márgenes laterales de 24dp -->
    <EditText
        android:id="@+id/edtUsuario"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:hint="Usuario"
        android:inputType="text"
        app:layout_constraintTop_toBottomOf="@id/txtTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo de entrada para Contraseña (Incremento 4): tipo textPassword para ocultar caracteres -->
    <EditText
        android:id="@+id/edtPassword"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:hint="Contraseña"
        android:inputType="textPassword"
        app:layout_constraintTop_toBottomOf="@id/edtUsuario"
        app:layout_constraintStart_toStartOf="@id/edtUsuario"
        app:layout_constraintEnd_toStartOf="@id/btnMostrarPassword" />

    <!-- Botón de imagen para mostrar u ocultar la contraseña -->
    <ImageButton
        android:id="@+id/btnMostrarPassword"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_visibility_24"
        android:background="@android:color/transparent"
        android:contentDescription="Mostrar u ocultar contraseña"
        android:onClick="onMostrarPasswordClick"
        app:layout_constraintTop_toTopOf="@id/edtPassword"
        app:layout_constraintBottom_toBottomOf="@id/edtPassword"
        app:layout_constraintEnd_toEndOf="@id/edtUsuario" />

    <!-- Casilla de verificación Recordarme (Incremento 5): debajo del campo contraseña -->
    <CheckBox
        android:id="@+id/chkRecordarme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Recordarme"
        app:layout_constraintTop_toBottomOf="@id/edtPassword"
        app:layout_constraintStart_toStartOf="@id/edtPassword" />

    <!-- Botón Ingresar (Incremento 6 y 7): ejecuta el método onIngresarClick al presionarse -->
    <Button
        android:id="@+id/btnIngresar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Ingresar"
        android:onClick="onIngresarClick"
        app:layout_constraintTop_toBottomOf="@id/chkRecordarme"
        app:layout_constraintStart_toStartOf="@id/edtUsuario"
        app:layout_constraintEnd_toEndOf="@id/edtUsuario" />

    <!-- Botón Limpiar: limpia los campos de texto y el checkbox al ejecutar onLimpiarClick -->
    <Button
        android:id="@+id/btnLimiar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:onClick="onLimpiarClick"
        android:text="Limpiar"
        android:backgroundTint="#FF0000"
        app:layout_constraintEnd_toEndOf="@+id/btnIngresar"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/btnIngresar"
        app:layout_constraintTop_toBottomOf="@+id/btnIngresar" />

</androidx.constraintlayout.widget.ConstraintLayout>